## About
- 本文件用于提示AI agent如何恰当地贡献本项目，在你进行任何修改前，**务必确保你理解了本文档的所有内容！**
- 本项目的用户手册在docs/BaneWfn_Project_Manual_zh.md

## Main
- 本项目主要开发环境为Ubuntu24.04（gcc=13.3）；但本项目同样需要在Rocky8下正常编译（gcc=8.4）运行，应考虑系统环境兼容性
- 若你在本项目任何地方见到符号链接，请勿清理，这通常是开发人员使用的
- 不应因为你看到的源码包里没有.gitignore等文件就推定我本地也没有，很可能只是tar打包目录时漏掉了.gitignore文件

## Delivery
- 明确在回复中声明修改了什么文件、具体什么行为发生了变化，由什么变成什么
- 向用户提供源码包和以原始上传源码包为baseline的patch补丁，确保**build等构建目录不要包含在patch内**
- 如果经历了多轮修改，除以原始上传源码包为baseline的patch以外，额外提供一份以上一次修改后的源码包为baseline的patch
- 若用户要求撤回修改A，同时提供已打过修改A补丁和未打过修改A补丁的patch
- patch应当采用Git unified diff format，类似diff --git a/foo.c b/foo.c，可以在项目根目录直接git apply
- 涉及到多个项目修改时，请就每个项目分别给出patch
- 即使用户给的任务很大，你无法一次完成，也要给出当前pre-alpha版本的patch和源码包，让我可以了解你的进度，把握下一步的方向

## Testing
- 修改源码后，至少应当跑通测试再交付；必要时可以添加新的测试
- 添加测试前，思考对于当前目的是否**确实**有必要添加一个测试。
  - 典型反例：修改了help内容后，加一个测试‘确定help内容确实改变了’是非常愚蠢的行为
  - 典型反例：修改schema version到0.6，加一个测试只认这个0.6，于是以后每次更新都要维护这个毫无价值的版本号检查
- 添加的测试应尽可能少地依赖外部文件

## Docs 
- 如果行为有变化，修改用户手册里相应部分
- 如需要修改手册，首先阅读docs/CONTRIBUTING.md
- 如果可能涉及大量文本增加而不是仅零星微调，解包docs/avoid-ai-flavor-writing-skill.zip并在写作过程中使用
- **严禁**将更新历史性质的文本写进用户手册，任何需要记录的更新历史均应当写入docs/UPDATE_HISTORY.md
- 为了到处引用而随意新建文档是**极其不受欢迎**的行为，你应当尝试将该讲的写进用户手册
- 如果确实有**仅**需要提示开发者而不适合给用户看的内容，新建文档是可以被允许的

## Readme
- README.md是一份**宣传用的页面**而不是说明书，牢记这个职责，它只讲如何上手本项目
- 将任何feature单独拿出来讲的内容均**不应该**出现在readme中
- **严禁**将路人根本没兴趣了解的代码细节写进readme
- 与进阶操作细节有关的内容，**一律写进用户手册**
- 如果可能涉及大量文本增加而不是仅零星微调，解包docs/avoid-ai-flavor-writing-skill.zip并在写作过程中使用
- 将不重要的改动急头白脸地写入readme是**极其不受欢迎**的行为，如果这份文档没有因更新产生事实错误，**那就不要更改它**

## Update
- 代码进行BUG修复后，更新小版本号（如：1.3.0 → 1.3.1）
- 代码进行功能性修改后，更新大版本号（如：1.3.0 → 1.4.0）
- 如果用户没有明确声明，不要自主修改主版本号（1.3.0 -x→ 2.0.0）
- 需要更新版本号的位置有：用户手册; CMakeLists.txt; README.md; src/ui.cpp; src/bwpack.rc; src/banewfn.rc; inno_setup/banetask.iss
- 更新历史写进docs/UPDATE_HISTORY.md
