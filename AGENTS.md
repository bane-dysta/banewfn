## About
- 本文件用于提示AI agent如何恰当地贡献本项目，在你进行任何修改前，**务必确保你理解了本文档的所有内容！**
- 本项目的用户手册在docs/BaneWfn_Project_Manual_zh.md

## Main
- 本项目主要开发环境为Ubuntu24.04；但本项目同样需要在Rocky8下正常编译（gcc=8.4）运行。你不必在容器中尝试用旧的gcc编译，但编写代码时应考虑系统环境兼容性。
- 除非用户明确要求，否则不用理会任意符号链接，请相信开发者已确保本地可用且发布时可以妥善处理相关问题，**请勿进行任何多余操作**，包括但不限于删除、移动、新建、重命名、提醒用户修改等。**除非用户明确要求，否则任何包含操作符号链接文件的补丁将被无条件拒绝。**
- 不应因为你看到的源码包里没有.gitignore等文件就推定我本地也没有，很可能只是tar打包目录时漏掉了.gitignore文件
- 如果你需要修改的文本涉及下列情况：1）会直接打印给用户；2）需要保存成面向人类的文档；3）是给用户而非AI看的静态文档；4）以其他常见方式构成用户可见；
  则你需要解包docs/agent/avoid-ai-flavor-writing-skill.zip，并在生成文本过程中使用该skill
- 用户的要求中可能存在非常明显的拼写错误/明显略微偏离现有接口的叙述，此时Agent应当在开始修改前指出这一点并声明自己会更正，以便于用户核对是否真的是拼写问题，切勿在发现明显的拼写错误之后还要去加一些很难看的、完全没有合理性的兼容策略，将错误固化进接口
  - 示例用户要求：'帮我给banewfn加个hepl命令，打印帮助' -- hepl很明显是拼写错误，此时绝对不应该直接去单独加一个hepl命令打印帮助
  - 示例用户要求：'我想让banewfn可以将所有reference.bib里的信息合并' -- 简单查阅代码便知banewfn默认输出到references.bib，显然是用户记错了单复数形式，因为让这个单复数去承载一整段逻辑的含义是明显不合理的

## snapshot
- **成果保存优先于完成状态。** 只要已经发生实质性代码修改，就不得以“尚未完成”“尚未编译”“测试未运行”或“测试失败”为理由扣住patch。应先交付可恢复的WIP/preview snapshot，再说明未完成项。
- 你应当理解**一旦本次对话结束，容器内的所有内容都将清空**。即，执行环境、容器和工作目录一律视为临时资源。仅告知用户`/mnt/data/work_*`等路径不算交付；必须在同一轮回复中提供实际可下载的patch，复杂或高价值任务还必须同时提供源码压缩包。
- 下列时点必须生成新的snapshot：
  - 每完成一个实质性实现里程碑后；
  - 运行耗时较长的构建或测试之前；
  - 执行reset、clean、rebase、批量删除、重建工作树等可能破坏现状的操作之前；
  - 准备在回复中声明任务尚未完成之前；
  - 本轮工作即将结束但用户要求的实现仍未完成时。
- snapshot文件不得覆盖先前版本，应使用递增序号或时间戳，例如`banetask-WIP-001.patch`、`banetask-WIP-002.patch`。
- WIP patch只用于保存和展示当前工作，不指定格式要求，具体操作细节见[snapshot.md](docs/agent/snapshot.md)：
- 每次交付时须说明它是WIP还是Delivery、所用baseline、是否包含新增文件、当前构建/测试状态、已知失败和未完成事项。
- WIP patch不要求通过构建或测试，但应尽可能在干净baseline上运行`git apply --check`，确认它至少可以完整应用。

## Update
- 代码进行BUG修复后，更新小版本号（如：1.3.0 → 1.3.1）
- 代码进行功能性修改后，更新大版本号（如：1.3.0 → 1.4.0）
- 如果用户没有明确声明，不要自主修改主版本号（1.3.0 -x→ 2.0.0）
- 需要更新版本号的位置有：用户手册; CMakeLists.txt; README.md; src/common/ui.cpp; packaging/windows/resources/bwpack.rc; packaging/windows/resources/banewfn.rc; packaging/windows/inno/banewfn.iss
- 更新历史写进docs/UPDATE_HISTORY.md

## Testing
- 修改源码后(手册不算)，至少应当跑通测试再交付；必要时可以添加新的测试
- 添加测试前，思考对于当前目的是否**确实**有必要添加一个测试。
  - 典型反例：修改了help内容后，加一个测试确定‘help内容确实改变了’是非常愚蠢的行为
  - 典型反例：修改schema version到0.6，加一个测试只认这个0.6，于是以后每次更新都要维护这个毫无价值的版本号检查
- 添加的测试应尽可能少地依赖外部文件

## Docs 
- 如果行为有变化，修改用户手册里相应部分
- 如需要修改手册，首先阅读docs/CONTRIBUTING.md
- **严禁**将更新历史性质的文本写进用户手册，任何需要记录的更新历史均应当写入docs/UPDATE_HISTORY.md
- 为了到处引用而随意新建文档是**极其不受欢迎**的行为，你应当尝试将该讲的写进用户手册
- 如果确实有**仅**需要提示开发者而不适合给用户看的内容，新建文档是可以被允许的

## Readme
- README.md是一份**宣传用的页面**而不是说明书，牢记这个职责，它只讲如何上手本项目
- 将任何feature单独拿出来讲的内容均**不应该**出现在readme中
- **严禁**将路人根本没兴趣了解的代码细节写进readme
- 与进阶操作细节有关的内容，**一律写进用户手册**
- 将不重要的改动急头白脸地写入readme是**极其不受欢迎**的行为，如果这份文档没有因更新产生事实错误，**那就不要更改它**

## Delivery
- 明确在回复中声明修改了什么文件、具体什么行为发生了变化，由什么变成什么
- 正确区分WIP patch和Delivery patch。前者不得以**包括但不限于尚未完成、尚未验证等任何理由扣下不发**；而后者在未经经过下述验证之前不得标注为交付Delivery patch：
  - 交付时，向用户提供源码包和以原始上传源码包为baseline的patch补丁，确保**build等构建目录不要包含在patch内**
  - 如果经历了多轮修改，交付时除以原始上传源码包为baseline的patch以外，额外提供一份以上一次修改后的源码包为baseline的patch
  - 若用户要求撤回修改A，则交付时同时提供已打过修改A补丁和未打过修改A补丁的patch
  - 交付时，atch应当采用Git unified diff format，类似diff --git a/foo.c b/foo.c，可以在项目根目录直接git apply
- 涉及到多个项目修改时，交付时请就每个项目分别给出patch
- patch内无需包含二进制pdf的修改
